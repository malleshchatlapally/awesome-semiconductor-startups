# Create an app at https://developer.x.com/ and copy the Bearer Token.
X_BEARER_TOKEN=your_bearer_token_here

# Comma-separated X usernames (no @). Defaults shown below if unset.
# KTR_HANDLES=KTRBRS,KTRoffice
